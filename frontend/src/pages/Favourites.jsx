import '../CSS/Favourites.css';

function favourite() {
    return <div className="favourite-empty">
        <h2>No favourites yet</h2>
        <p> Need to work on Favourites when there is time</p>
    </div>
}

export default favourite